{
  "hooks": {
    "PreCommit": [
      {
        "command": "bash -c 'cd \"$(git rev-parse --show-toplevel)\" && ./scripts/check-vault-hook.sh'",
        "description": "Validate Obsidian vault integrity. Informational under 15 notes; blocks commit on warnings at 15+ notes.",
        "enabled": true
      }
    ],
    "SessionStart": [
      {
        "command": "bash -c 'cd \"$(git rev-parse --show-toplevel)\" && ./scripts/session-sync.sh 2>/dev/null || true'",
        "description": "Fetch from origin and report sync status (behind, ahead, diverged, recent activity).",
        "enabled": true
      },
      {
        "command": "bash -c 'cd \"$(git rev-parse --show-toplevel)\" && ./scripts/validate-vault.sh 2>/dev/null || true'",
        "description": "Show vault health on session start (informational, never blocks).",
        "enabled": true
      }
    ]
  }
}
